# 使用 CPU 版 PyTorch（避免下载几百 MB 的 CUDA 库）
--extra-index-url https://download.pytorch.org/whl/cpu

# FastAPI 后端
fastapi==0.141.1
uvicorn==0.52.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# LangChain 生态（锁定实测版本，保证构建可复现）
langchain==1.3.14
langchain-openai==1.4.1
langchain-community==0.4.2
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2
langchain-core==1.5.3

# 研究助手 Agent（Web 搜索 + Postgres 长短期记忆）
langchain-tavily==0.2.18
langgraph==1.2.10
langgraph-checkpoint-postgres==3.1.1
psycopg[binary,pool]==3.3.4

# Redis（LLM 响应缓存，同步 + 异步客户端同属此包）
redis==8.1.0

# 异步 HTTP 客户端（/balance 直接使用，显式声明不依赖传递安装）
httpx>=0.27.0

# Prometheus 监控：instrumentator 自动采 HTTP 指标，prometheus-client 业务埋点（metrics.py）
prometheus-client==0.26.0
prometheus-fastapi-instrumentator==8.1.0

# 向量数据库（Milvus）
pymilvus==3.0.1
langchain-milvus==0.4.0

# PDF 解析（MinerU 本地 Python API，锁定实测 3.4.4）
mineru==3.4.4

# BM25 混合检索
rank_bm25>=0.2.2
jieba==0.42.1

# 网页解析
beautifulsoup4>=4.12.0

# 结构化日志（JSON + stdout，request_id 链路）
structlog==26.1.0

# 嵌入模型
sentence-transformers==5.6.1

# 重依赖显式锁定（传递依赖，锁定后版本不再随重建漂移）
torch==2.13.0+cpu
transformers==5.14.1
