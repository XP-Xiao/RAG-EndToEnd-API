# Grafana 数据源自动装配：容器启动时自动添加 Prometheus，无需手动在 UI 里配置
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus              # 固定 uid，供预置仪表盘 JSON 引用
    type: prometheus
    access: proxy
    url: http://prometheus:9090   # 容器间通过服务名通信
    isDefault: true
    editable: true
